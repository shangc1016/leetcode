#ifndef __GREEDY_H_
#define __GREEDY_H_

#include <string>
#include <vector>

using namespace std;

namespace leetcode {
int findContentChildren(vector<int>& g, vector<int>& s);

int wiggleMaxLength(vector<int>& nums);
}

#endif